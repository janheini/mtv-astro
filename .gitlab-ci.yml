image: node:lts

before_script:
  - npm install --cache .npm --prefer-offline

pages:
  only:
    - main
  artifacts:
    paths:
      - public
  variables:
    SITE: $CI_PAGES_URL
    BASE: $CI_PROJECT_NAME
  cache:
    key:
      files:
        - package-lock.json
    paths:
      - public/
      - .npm/
      - node_modules/
  script:
    - LANG=de_DE.UTF-8 npm run build
